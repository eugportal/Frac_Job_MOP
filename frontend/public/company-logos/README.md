# Company logos

Add each logo here using the exact company name returned at login, for example
`Kuwait Petroleum Company.png`. PNG, SVG, JPG, JPEG, and WebP files are supported.
The logo appears automatically after the user signs in; the company name remains
visible beside it as an accessible fallback.

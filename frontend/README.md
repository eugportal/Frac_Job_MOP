# Frac Job Frontend

The React/Vite client for the Frac Job application.

## Run locally

```powershell
cd frontend
npm run dev
```

Copy `.env.example` to `.env` and set `VITE_API_URL` when the API is not available at `http://localhost:3001`.

## Verify

```powershell
npm run typecheck
npm run build
```
